_site/
push.sh
.DS_Store

# original reference résumé at repo root (kept locally, not published)
/Swayam-Singh-Resume.pdf

# LaTeX résumé build artifacts (source is resume/resume.tex; served PDF in files/)
resume/*.aux
resume/*.log
resume/*.out
resume/*.fls
resume/*.fdb_latexmk
resume/resume.pdf

# Local Jekyll / Ruby build artifacts
vendor/
.bundle/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
